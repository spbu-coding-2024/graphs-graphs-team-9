package viewModel.toosl

import androidx.compose.ui.graphics.Color
import kotlin.random.Random

object CoolColors {
    val edgeBasic = Color.Gray

    val vertexBasic = Color.Gray
    val backgroundBasic = Color(225,213,213)
    val pathHighlightVertex = Color(93, 167, 250)
    val pathHighlightEdge = Color.Blue
}